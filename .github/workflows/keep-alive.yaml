name: Keep Server Alive

on:
  schedule:
    - cron: '7,17,27,37,47,57 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Ping server with curl
        run: |
          curl -f -sS -L https://book-review-lbg7.onrender.com/ -o /dev/null